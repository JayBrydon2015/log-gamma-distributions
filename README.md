# log-gamma-distributions

Analysis into how well normal distributions approximate log-gamma distributions. The plots produced give insight. Simply alter the parameters `alpha` and `beta` of the gamma distribution, keep the sample `size` large, and run the code.
