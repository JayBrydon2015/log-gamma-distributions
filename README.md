# log-gamma-distributions

Analysis into how well log-gamma distributions are approximated by normal distributions. The code produces plots that give insight into this. Simply alter the parameters `alpha` and `beta` of the gamma distribution, keep the sample `size` large, and run the code.
